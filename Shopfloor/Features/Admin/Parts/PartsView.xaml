<UserControl
    x:Class="Shopfloor.Features.Admin.Parts.PartsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Shopfloor.Features.Admin.Parts"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:PartsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel Width="400">
            <TextBlock Text="Name" />
            <TextBox Text="{Binding Path=PartTypeName}" />
            <Button
                Command="{Binding Path=PartTypeCreateCommand}"
                CommandParameter="{Binding Path=PartType}"
                Content="STWÓRZ" />
        </StackPanel>
    </Grid>
</UserControl> 