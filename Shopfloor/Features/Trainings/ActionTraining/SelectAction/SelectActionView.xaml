<UserControl x:Class="Shopfloor.Features.Trainings.ActionTraining.SelectAction.SelectActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Shopfloor.Features.Trainings.ActionTraining.SelectAction"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Padding="0,5"
            HorizontalAlignment="Center"
            FontSize="32"
            FontWeight="ExtraBold"
            Foreground="{StaticResource BrushPurple500}"
            Text="WYBIERZ DZIAŁANIE"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical"
                        Grid.Column="0"
                        Width="250px">

                <TextBlock Style="{StaticResource TitleTextBlock}"
                           Text="Filtry"/>

                <Border>
                    <StackPanel>
                        <TextBlock
                            Margin="0,7,0,3"
                            Style="{StaticResource TooltipText}"
                            Text="Linia"/>
                        <ComboBox
                            MinHeight="30px"
                            DisplayMemberPath="Name"
                            IsEditable="true"
                            IsReadOnly="false"
                            IsTextSearchEnabled="false"
                            ItemsSource="{Binding Path=Lines}"
                            SelectedItem="{Binding Path=SelectedLine}"
                            SelectedValuePath="Name"
                            Style="{StaticResource ComboBoxMain}"
                            Text="{Binding FilterData.Line, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Border>

                <Border>
                    <StackPanel>
                        <TextBlock
                            Margin="0,7,0,3"
                            Style="{StaticResource TooltipText}"
                            Text="Maszyna"/>
                        <ComboBox
                            MinHeight="30px"
                            DisplayMemberPath="Name"
                            IsEditable="true"
                            IsReadOnly="false"
                            IsTextSearchEnabled="false"
                            ItemsSource="{Binding Path=Machines}"
                            SelectedItem="{Binding Path=SelectedMachine}"
                            SelectedValuePath="Name"
                            Style="{StaticResource ComboBoxMain}"
                            Text="{Binding FilterData.Machine, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <ScrollViewer
                x:Name="OuterScrollerViewer"
                Grid.Column="1"
                Width="auto"
                MaxHeight="700px"
                Margin="0,15,0,0"
                PreviewMouseWheel="DataGrid_PreviewMouseWheel">

                <DataGrid
                    Margin="0,0,10,0"
                    AutoGenerateColumns="False"
                    CellStyle="{DynamicResource DataGridCellMain}"
                    ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderMain}"
                    IsReadOnly="True"
                    ItemsSource="{Binding Path=Activities}"
                    SelectedItem="{Binding Path=Activity}"
                    SelectionMode="Single"
                    SelectionUnit="FullRow"
                    Style="{DynamicResource DataGridMain}"
                    RowStyle="{StaticResource DataGridRowMain}"
                    TargetUpdated="DataGrid_TargetUpdated">

                    <DataGrid.Columns>

                        <DataGridTextColumn
                            Width="auto"
                            Binding="{Binding Machine.Line.Name}"
                            Header="Warsztat"/>

                        <DataGridTextColumn
                            Width="auto"
                            Binding="{Binding Machine.Name}"
                            Header="Warsztat"/>

                        <DataGridTextColumn
                            Width="auto"
                            Binding="{Binding Workshop.Name}"
                            Header="Warsztat"/>

                        <DataGridTextColumn
                            Width="auto"
                            Binding="{Binding Type.Name}"
                            Header="Rodzaj"/>

                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Description, NotifyOnTargetUpdated=True}"
                            Header="Opis"/>

                        <DataGridTextColumn
                            Width="auto"
                            Binding="{Binding Additionals}"
                            Header="Specjalne"/>

                        <DataGridTemplateColumn
                            Width="auto"
                            Header="Akcje"
                            IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">

                                        <Button
                                            Command="{Binding Path=DataContext.ChooseActionCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource gridConfirmButton}">
                                            <TextBlock
                                                FontFamily="Segoe MDL2 Assets"
                                                Style="{StaticResource gridButtonIcon}"
                                                Text="&#xE7B3;"/>
                                        </Button>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl> 