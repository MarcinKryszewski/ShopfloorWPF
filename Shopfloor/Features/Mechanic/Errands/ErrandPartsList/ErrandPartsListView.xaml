<UserControl x:Class="Shopfloor.Features.Mechanic.Errands.ErrandPartsList.ErrandPartsListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Shopfloor.Features.Mechanic.Errands.ErrandPartsList"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Red">
        <DataGrid ItemsSource="{Binding Path=ErrandParts}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="auto"
                    Binding="{Binding Path=Part.Id}"
                    CanUserResize="False"
                    Header="#"
                    IsReadOnly="True"
                     />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Path=Part.NamePl}"
                    Header="Nazwa"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Path=Amount}"
                    Header="Ilość"
                    IsReadOnly="False" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl> 